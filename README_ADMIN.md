# CR7 ELITE - Admin Integrated

تم تحديث المشروع ليشمل لوحة تحكم إدارية كاملة مدمجة داخل نفس المشروع.

## 🚀 التحديثات الجديدة

1. **نظام دخول المسؤول اليدوي**:
   - المسار: `/admin/login`
   - اسم المستخدم الافتراضي: `admin`
   - كلمة المرور الافتراضية: `Nador@2009`
   - يتم التحقق من البيانات مقابل متغيرات البيئة `VITE_ADMIN_USERNAME` و `VITE_ADMIN_PASSWORD`.

2. **لوحة التحكم المدمجة**:
   - تم دمج كافة صفحات الإدارة (المستخدمين، السحوبات، VIP، التحديات) داخل المشروع الحالي.
   - حماية المسارات: لا يمكن الوصول لأي مسار يبدأ بـ `/admin` إلا بعد تسجيل الدخول كمسؤول.

3. **إدارة البيانات الحقيقية**:
   - مرتبطة مباشرة بجداول Supabase: `profiles`, `crypto_withdrawals`, `vip_tiers`, `challenges`.
   - إمكانية تعديل الأرصدة، ترقية VIP، وقبول/رفض السحوبات.

## 🛠 التشغيل المحلي

```bash
npm install
npm run dev
```

## 🔒 متغيرات البيئة المطلوبة (Vercel/Local)

تأكد من إضافة المتغيرات التالية في إعدادات Vercel:
- `VITE_ADMIN_USERNAME=admin`
- `VITE_ADMIN_PASSWORD=Nador@2009`
- بالإضافة إلى متغيرات Supabase الموجودة مسبقاً.

## 📂 هيكل الملفات الجديد

- `src/pages/admin/Login.tsx`: صفحة تسجيل الدخول.
- `src/pages/admin/VIP.tsx`: إدارة باقات VIP.
- `src/pages/admin/Challenges.tsx`: إدارة التحديات.
- `src/components/layout/AdminLayout.tsx`: نظام الحماية والقائمة الجانبية.
