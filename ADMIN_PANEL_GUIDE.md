# دليل لوحة تحكم Admin Panel - CR7 ELITE

تم تطوير لوحة تحكم احترافية متكاملة لإدارة منصة الاستثمار CR7 ELITE.

## 🚀 المميزات الرئيسية

1. **Dashboard ذكية**:
   - عرض إحصائيات المستخدمين، الأرصدة، وطلبات السحب المعلقة.
   - رسوم بيانية تفاعلية لمتابعة نمو المنصة.

2. **إدارة المستخدمين**:
   - البحث عن المستخدمين بالاسم أو البريد.
   - تعديل الأرصدة (إضافة/خصم) مع تسجيل العملية في التاريخ.
   - ترقية مستويات VIP (من 0 إلى 7).
   - تغيير أدوار المستخدمين (User / Admin).

3. **نظام السحب (Withdraw System)**:
   - قائمة شاملة بطلبات السحب مع فلاتر للحالة.
   - إمكانية الموافقة (Approve) أو الرفض (Reject).
   - استرداد تلقائي للمبلغ في حال الرفض.
   - ربط جاهز مع NOWPayments Payout API.

4. **سجل النشاطات (Activity Log)**:
   - تتبع كامل لجميع العمليات التي يقوم بها المدراء لضمان الأمان.

5. **الإعدادات العامة**:
   - إدارة مفاتيح API الخاصة بـ NOWPayments.
   - تفعيل/تعطيل IP Whitelist و 2FA.

## 🛠 المتطلبات التقنية

- **Frontend**: React + Vite + TailwindCSS + Shadcn/UI.
- **Backend**: Supabase (Auth, Database, Edge Functions).
- **Icons**: Lucide React.
- **Charts**: Recharts.

## 📦 كيفية التثبيت

1. قم بتشغيل ملف الهجرة المرفق في Supabase SQL Editor:
   `supabase/migrations/20260204000000_admin_setup.sql`

2. لتعيين مستخدم كمدير (Admin)، قم بتشغيل الأمر التالي في SQL Editor:
   ```sql
   UPDATE profiles SET role = 'admin' WHERE username = 'admin';
   ```

3. قم بتشغيل المشروع محلياً:
   ```bash
   npm install
   npm run dev
   ```

## 🔒 الأمان
- جميع مسارات الإدارة محمية بواسطة `AdminLayout` الذي يتحقق من دور المستخدم في قاعدة البيانات.
- سياسات RLS في Supabase تمنع أي مستخدم غير مدير من الوصول إلى بيانات الإدارة أو الإعدادات.
